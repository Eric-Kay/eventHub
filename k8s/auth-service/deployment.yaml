apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: eventhub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
      - name: auth-service
        image: your-ecr/auth-service:latest
        ports:
        - containerPort: 8000
        env:
        - name: POSTGRES_DB
          value: eventhub
        - name: POSTGRES_USER
          value: eventhub
        - name: POSTGRES_PASSWORD
          value: eventhub123
        - name: POSTGRES_HOST
          value: postgres
        - name: REDIS_HOST
          value: redis
        - name: RABBITMQ_HOST
          value: rabbitmq

---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: eventhub
spec:
  selector:
    app: auth-service
  ports:
  - port: 8000
    targetPort: 8000
